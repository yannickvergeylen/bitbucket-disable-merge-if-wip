{
  "manifest_version": 2,

  "name": "Disable merge if wip on gitlab",
  "version": "0.1.0",
  "description": "Simple extension to prevent user errors on bitbucket",

  "content_scripts": [{
    "js": ["index.js"],
    "matches": ["https://bitbucket.org/*"]
  }]

}
